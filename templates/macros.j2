<?xml version="1.0"?>
<!-- {{ ansible_managed }} -->
<clickhouse>
  <macros>
{% for key, value in clickhouse_macros.items() %}
    <{{ key }}>{{ value }}</{{ key }}>
{% endfor %}
  </macros>
</clickhouse>
